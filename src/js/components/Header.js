export default class Header {
  constructor() {

  }

  render(props) { //при вызове перерисовывает шапку в зависимости от переданного аргумента — объекта props

  }
}

// props = {
  // isLoggedIn: false,
  // userName: 'Zak';
// }